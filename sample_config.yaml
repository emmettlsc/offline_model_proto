project:
  name: offline_model_proto

runtime:
  device: auto              # auto | cpu | cuda
  confidence_threshold: 0.5

paths:
  model_dir: models
  data_dir: data
  output_dir: outputs

video:
  frame_stride: 30
  max_frames: 100
