# =============================================================================
# WARNING: This file is a placeholder, NOT a real lock file.
# =============================================================================
#
# A locked, Linux-compatible requirements.txt MUST be generated on a Linux
# machine that matches the target air-gapped environment (Python minor version,
# glibc, architecture, CUDA toolkit).
#
# A macOS-generated lock is NOT safe to ship — it will pin macOS-only wheels
# (filenames containing macosx_*) and may resolve to different versions than
# Linux would pick.
#
# To regenerate on the Linux build host:
#
#     python3 -m venv venv
#     source venv/bin/activate
#     pip install --upgrade pip setuptools wheel pip-tools
#     pip-compile requirements.in -o requirements.txt
#
# Until then, fall back to the unlocked input file on dev machines:
#
#     pip install -r requirements.in
#
# See docs/LINUX_WHEELHOUSE_BUILD.md for the full procedure, including CUDA
# torch builds.
# =============================================================================
